git init // co este comando se inicia el repositorio en la carpeta que se encuentra el editor en este caso la carpeta unidad 2 

git remote add origin https://github.com/Lopz532/Programadores.git // este comando es para agregar la conexion remota de su repositorio creado en github debe ser identico ya que de lo contrario les dara errores si en caso les pasa pueden removerlo con el siguiente comando 

git remote remove origin // con este comando remueven la conexion remota 

git add . // con este al iniciar es el punto por que no se tiene ningun archivo pero en caso contrario es para agregar archivos al commit

git commit -m "chore: initial commit - react base" // con este comando se agregan los commits -m significa el mensaje del commit puede ser cualquier cosa pero de preferencia lo que se esta agregando

git branch -M main // este comando renombra la rama principal de tu proyecto de master a main (master se usaba antes en github ahora es main), esto solo se hace una vez

git push -u origin main // al ya tener agregado archivos en el commit se ejecuta este comando para subir los archivos en el repo de github ya que origin es su lap o donde estan editando y el main es hacia donde se va a subir 



ATAJOS ctrl + c para cancelar el comando tambien usado para copiar 
ATAJOS ctrl + v para pegar
ATAJOS ctrl + x para borrar
ATAJOS ctrl + ñ para abrir la terminal 
ctrl + s para guardar

les explicare si son los mismos comandos para el cmd o powershell ya que como ven uso otro tipo de terminal es de git bash